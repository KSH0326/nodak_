<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nodak_">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nodak_">
        <activity android:name=".LoginActivity" android:label="로그인"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"/>

        <activity android:name=".secondActivity" android:label = "image액티비티"/>
        <activity android:name=".CalendarActivity" android:label = "캘린더액티비티"/>
        <activity android:name=".CalendaerActivity_re" android:label="캘린더 수정"/>



    </application>



</manifest>